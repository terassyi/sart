# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/sart.terassyi.net_clusterbgps.yaml
- bases/sart.terassyi.net_bgppeers.yaml
- bases/sart.terassyi.net_nodebgps.yaml
- bases/sart.terassyi.net_bgpadvertisements.yaml
- bases/sart.terassyi.net_addresspools.yaml
- bases/sart.terassyi.net_addressrequests.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_clusterbgps.yaml
#- patches/webhook_in_bgppeers.yaml
#- patches/webhook_in_nodebgps.yaml
#- patches/webhook_in_bgpadvertisements.yaml
#- patches/webhook_in_addresspools.yaml
#- patches/webhook_in_addressrequests.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_clusterbgps.yaml
#- patches/cainjection_in_bgppeers.yaml
#- patches/cainjection_in_nodebgps.yaml
#- patches/cainjection_in_bgpadvertisements.yaml
#- patches/cainjection_in_addresspools.yaml
#- patches/cainjection_in_addressrequests.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
