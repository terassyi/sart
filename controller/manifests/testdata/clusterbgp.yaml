apiVersion: sart.terassyi.net/v1alpha2
kind: ClusterBgp
metadata:
  name: clusterbgp-sample
spec:
  policy:
    nodeSelector:
      matchLabels:
        bgp: a
    asnSelector:
      from: label
    endpoint_port: 5000
